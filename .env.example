# Django
SECRET_KEY=your-secret-key-here

# PostgreSQL Database
DB_NAME=kisan_db
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# AI (Optional - Groq is used as fallback if Ollama is unavailable)
GROQ_API_KEY=your-groq-api-key-here

# Debug (set to False in production)
DEBUG=True
